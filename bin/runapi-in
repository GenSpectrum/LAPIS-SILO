#!/bin/bash
set -meuo pipefail
IFS=

usage() {
    echo "$0 test-input-dir port-number" >&2
    false
}

if [[ $# != 2 ]]; then
    usage
fi
testdir="$1"
portnumber="$2"


set -x

cd "$testdir"

export SPDLOG_LEVEL=${SPDLOG_LEVEL-debug}
exec ../../build/siloApi --api --port "$portnumber"
