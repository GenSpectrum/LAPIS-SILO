#!/bin/bash
set -meuo pipefail
IFS=

usage() {
    echo "$0 test-input-dir" >&2
    false
}

if [[ $# != 1 ]]; then
    usage
fi
if [[ "$1" == "-h" ]] || [[ "$1" == "--help" ]]; then
    usage
fi
testdir="$1"
mypath=$(readlink -f "$0")
mydir=$(dirname "$mypath")
builddir=$(dirname "$mydir")/build

IFS=' '

set +x

rm -rf temp

cd "$testdir"

rm -rf {output,temp,test?*}


#SPDLOG_LEVEL=debug
${DEBUGGER-} "$builddir"/siloApi --preprocessing
