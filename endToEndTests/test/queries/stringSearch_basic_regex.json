{
  "testCaseName": "StringSearch with a basic regex",
  "query": "default.filter(!(primary_key.like('key'))).groupBy({count:=count()})",
  "expectedQueryResult": [
    {
      "count": 0
    }
  ]
}
