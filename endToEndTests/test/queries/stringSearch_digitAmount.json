{
  "testCaseName": "StringSearch that matches the primary key to end with exactly six digits",
  "query": "default.filter(primary_key.like('^\\\\D*\\\\d{1}$')).groupBy({count:=count()})",
  "expectedQueryResult": [
    {
      "count": 9
    }
  ]
}
