{
  "testCaseName": "FloatBetween for column without bounds returns all non null values",
  "query": "default.filter(qc_value.isNotNull()).groupBy({count:=count()})",
  "expectedQueryResult": [
    {
      "count": 98
    }
  ]
}
