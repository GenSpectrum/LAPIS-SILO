{
  "testCaseName": "Get the unaligned fasta for one sequence",
  "query": "default.filter(primary_key = 'key_41').project({primary_key, unaligned_main})",
  "expectedQueryResult": [
    {
      "primary_key": "key_41",
      "unaligned_main": "some_very_short_string"
    }
  ]
}
