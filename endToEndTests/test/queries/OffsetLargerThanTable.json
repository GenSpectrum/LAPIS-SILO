{
  "testCaseName": "Offset is larger than the table",
  "query": "res.filter((country = 'Switzerland') && (pango_lineage.lineage('B.1.1.7', includeSublineages:=true))).details(offset:=1231241).orderBy('primary_key')",
  "expectedQueryResult": []
}
