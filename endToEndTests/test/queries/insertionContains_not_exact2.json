{
  "testCaseName": "Insertion Contains with non-exact match CAG.*AA",
  "query": "default.filter(insertionContains(position:=22204, value:='CAG.*AA')).groupBy({count:=count()})",
  "expectedQueryResult": [
    {
      "count": 1
    }
  ]
}
