{
  "testCaseName": "Get two unaligned fastas for one row",
  "query": "default.filter(primary_key = 'key_41').project({primary_key, unaligned_main, unaligned_testSecondSequence})",
  "expectedQueryResult": [
    {
      "primary_key": "key_41",
      "unaligned_main": "some_very_short_string",
      "unaligned_testSecondSequence": "AAGN"
    }
  ]
}
