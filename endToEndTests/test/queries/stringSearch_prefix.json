{
  "testCaseName": "StringSearch with a regex matching the prefix",
  "query": "default.filter(primary_key.like('^key_')).groupBy({count:=count()})",
  "expectedQueryResult": [
    {
      "count": 100
    }
  ]
}
