{
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check-format": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "chai": "^5.1.2",
    "prettier": "^3.5.0",
    "supertest": "^7.0.0"
  }
}
