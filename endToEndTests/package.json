{
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check-format": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "chai": "^5.1.1",
    "prettier": "^3.3.2",
    "supertest": "^7.0.0"
  }
}
